[project]
name = "aethon"
version = "0.1.0"
description = "A python cache package"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
